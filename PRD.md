# VOC Agent PRD (Product Requirements Document)

- 작성일: 2026-04-13
- 문서 버전: v1.0
- 대상: VOC Agent (Frontend + Backend RAG Service)
- 문서 목적: 현재 구현 상태를 기준으로 MVP 요구사항을 명확히 정의하고, 릴리스/운영 기준을 수립

---

## 1) 제품 개요

### 1.1 문제 정의
현재 기술지원 조직의 VOC 처리 프로세스는
- VOC 수집 채널 분산,
- 담당자별 대응 편차,
- 반복 이슈에 대한 가이드 재사용 어려움,
- 승인/배포 프로세스 지연
으로 인해 평균 대응 시간이 길고 품질 편차가 발생한다.

### 1.2 제품 비전
VOC Agent는 **VOC 수집 → 분석(RAG) → 가이드 생성(LLM) → 승인 → 배포**를 단일 워크플로로 통합하여,
- 대응 시간을 단축하고,
- 가이드 품질과 일관성을 높이며,
- 운영팀의 지식 자산화를 가속한다.

### 1.3 핵심 가치 제안
- 처리속도: 반복 이슈에 대한 초안 자동 생성으로 처리 리드타임 단축
- 품질일관성: 구조화된 템플릿(문제/원인/절차/해결/출처) 강제
- 운영가시성: 대시보드 기반 상태/승인 현황 추적
- 확장성: Jira Webhook + RAG 백엔드 + 프론트엔드 API 계층으로 모듈형 확장

---

## 2) 목표와 비목표

### 2.1 제품 목표 (MVP)
1. VOC를 표준 스키마로 수집/조회/검색 가능해야 한다.
2. VOC 상세에서 AI 가이드 생성 요청을 수행하고 구조화된 결과를 저장/조회할 수 있어야 한다.
3. 가이드 승인 워크플로(PENDING/APPROVED/REJECTED)를 운영 가능해야 한다.
4. 대시보드에서 운영 핵심 지표(총 VOC, 승인 대기/완료 등)를 확인할 수 있어야 한다.

### 2.2 비목표 (MVP 범위 외)
- 고도화된 권한 체계(RBAC 세분화)
- 멀티 테넌트 지원
- 완전 자동 게시(외부 고객 문서 플랫폼 양방향 동기화)
- 오프라인 학습 파이프라인 자동화(MLOps)

---

## 3) 대상 사용자 및 주요 시나리오

### 3.1 사용자 페르소나
- **Support Agent (1차 대응자)**: VOC 등록/상태 추적, AI 초안 확인
- **PDM/Reviewer (검토자)**: 가이드 품질 검토, 승인/반려
- **Ops Manager (운영 관리자)**: SLA/처리량/승인율 모니터링

### 3.2 핵심 사용자 여정
1. Jira Webhook 또는 수동 입력으로 VOC 유입
2. VOC 목록/검색에서 대상 VOC 확인
3. VOC 상세에서 AI 가이드 생성 실행
4. 생성된 가이드를 편집 후 승인 요청
5. Reviewer가 승인/반려 코멘트 처리
6. 승인 완료된 가이드가 운영 지식으로 축적

---

## 4) 범위 정의 (Functional Requirements)

### FR-01 VOC 수집 및 관리
- Jira Webhook 기반 VOC 수집 API 제공
- VOC 목록 조회/상세 조회 지원
- 필터링: 카테고리, 상태, 우선순위, 검색어
- 상태 전이(예: NEW → IN_PROGRESS → RESOLVED) 지원

**수용 기준 (Acceptance Criteria)**
- [ ] Webhook 요청 성공 시 VOC 레코드가 생성된다.
- [ ] 목록 API에서 필터 조건 조합 조회가 가능하다.
- [ ] 상세 화면에서 Jira Key, 본문, 우선순위, 상태가 표시된다.

### FR-02 AI 가이드 생성
- `VOC 제목 + 설명` 기반으로 RAG 검색을 수행한다.
- 검색 결과를 컨텍스트로 LLM이 구조화된 가이드를 생성한다.
- 출력 포맷: 문제(problem), 원인(cause), 절차(procedure), 해결(solution), 출처(sources)

**수용 기준**
- [ ] 가이드 생성 API 호출 시 구조화 필드가 모두 반환된다.
- [ ] 실패 시 표준 에러 포맷으로 사유를 반환한다.
- [ ] 생성 결과는 가이드 엔티티로 저장되고 VOC와 연결된다.

### FR-03 가이드 검토/승인 워크플로
- 가이드 상태: DRAFT, PENDING, APPROVED, REJECTED
- 승인 코멘트/반려 사유 기록
- 승인 이력 조회 가능

**수용 기준**
- [ ] 승인 이벤트 발생 시 상태가 즉시 변경된다.
- [ ] 반려 시 코멘트 필수 정책을 설정할 수 있다(운영 설정).
- [ ] 승인 이력(승인자, 시각, 코멘트) 조회가 가능하다.

### FR-04 대시보드 및 운영 지표
- KPI 카드: 총 VOC, 대기 가이드, 승인 가이드, 해결 VOC
- 최근 활동 피드
- 기간/상태 기준 통계 확장 가능 구조

**수용 기준**
- [ ] 페이지 로드 시 KPI 카드가 3초 이내 렌더링된다(로컬 기준).
- [ ] KPI 집계 로직이 VOC/Guide 상태값과 일치한다.

### FR-05 문서/검색 API
- RAG 백엔드는 `/search`, `/generate`, `/search-and-generate`, `/health` 제공
- 검색은 하이브리드(FAISS+BM25) 사용
- 기본 Top-K와 가중치(alpha) 환경변수로 조정 가능

**수용 기준**
- [ ] `/health`에서 모델/인덱스 상태를 확인할 수 있다.
- [ ] `/search`는 Top-K 제한과 유효성 검사를 준수한다.
- [ ] `/generate`는 API 키 미설정 시 명시적 오류를 반환한다.

---

## 5) 비기능 요구사항 (NFR)

### NFR-01 성능
- 평균 가이드 생성 시간: 30초 이내(표준 입력 기준)
- 대시보드 주요 API P95: 1초 이내(캐시 미적용 MVP 기준 목표)
- VOC 목록 API P95: 1.5초 이내

### NFR-02 신뢰성
- 주요 API 가용성 99.5% 이상(MVP 운영 목표)
- 외부 LLM 장애 시 재시도 또는 사용자 안내 메시지 제공

### NFR-03 보안
- API 키/DB 접속정보는 환경변수로만 관리
- Webhook 인증(시그니처 또는 토큰) 도입 필요
- 운영 배포 시 CORS 허용 도메인 제한 필요

### NFR-04 감사 가능성
- 가이드 상태 변경, 승인/반려, 수동 편집 이벤트 로깅
- 운영 이슈 추적을 위한 요청 ID/에러 코드 표준화

---

## 6) 정보구조 및 데이터 요구사항

### 6.1 핵심 엔티티
- User: 작성자/검토자
- VOC: 고객 이슈 원본
- Guide: VOC 기반 해결 가이드
- Approval: 승인 워크플로 이력
- Document: 검색 지식 원문

### 6.2 데이터 무결성 규칙
- Guide는 반드시 VOC에 종속된다.
- Approval은 반드시 Guide와 Approver(User)에 종속된다.
- VOC `jiraKey`, User `email`은 유니크해야 한다.

---

## 7) API 요구사항

### 7.1 Frontend BFF/Route Handler
- `/api/voc`, `/api/voc/[id]`, `/api/voc/webhook`
- `/api/guides`, `/api/guides/[id]`, `/api/guides/generate`
- `/api/approvals`, `/api/approvals/[id]`
- `/api/dashboard/stats`

### 7.2 Backend RAG Service
- `GET /health`
- `POST /search`
- `POST /generate`
- `POST /search-and-generate`

### 7.3 에러 정책 (권장)
```json
{
  "error": "Human-readable message",
  "code": "MACHINE_CODE",
  "requestId": "uuid"
}
```

---

## 8) UX 요구사항

1. 사용자는 3클릭 이내로 VOC 상세에 진입해야 한다.
2. 가이드 생성 버튼은 실행 상태(로딩/완료/실패)를 명확히 표시해야 한다.
3. 승인/반려 액션은 실수 방지를 위한 확인 UX를 제공해야 한다.
4. 목록 페이지는 검색/필터 상태를 URL 쿼리로 보존해야 한다.

---

## 9) 지표(KPI)와 성공 기준

### 9.1 제품 KPI
- 가이드 생성 사용률 = (AI 생성 실행 VOC 수 / 전체 VOC 수)
- 승인율 = (APPROVED 가이드 / 승인 요청 가이드)
- 평균 처리시간(TTR) = VOC 생성~해결까지 평균 시간
- 재오픈율 = 해결 후 재발생 VOC 비율

### 9.2 기술 KPI
- 검색 Recall@20 ≥ 0.90
- 가이드 생성 성공률 ≥ 95%
- API 오류율(5xx) ≤ 1%

---

## 10) 릴리스 계획

### Phase 1 (현재~MVP)
- VOC CRUD + 가이드 생성 + 승인 워크플로 + 대시보드 KPI
- 모의 데이터/실데이터 혼용 환경 안정화

### Phase 2
- 권한/인증 강화(JWT + 역할 기반 접근)
- 문서 게시 자동화(사내 문서 플랫폼 연동)
- 품질평가 자동화(가이드 정합성 스코어링)

### Phase 3
- 멀티 프로젝트/멀티 조직 확장
- 비용 최적화(캐싱, 모델 라우팅)
- A/B 테스트 기반 프롬프트 최적화

---

## 11) 리스크 및 대응

1. **LLM 응답 변동성**
   - 대응: 출력 스키마 검증 + 후처리 + 재시도
2. **지식베이스 최신성 부족**
   - 대응: 문서 증분 동기화 및 버전 태깅
3. **운영 보안 미흡(Webhook/CORS)**
   - 대응: 서명검증, 허용 도메인 화이트리스트, 비밀값 로테이션
4. **프론트-백엔드 응답 스키마 불일치**
   - 대응: OpenAPI/타입 계약 테스트 도입

---

## 12) 오픈 이슈 (의사결정 필요)

1. 승인 정책: 단일 승인 vs 다중 승인(2인 이상)
2. 가이드 게시 대상: 내부 전용 vs 고객 노출 버전 분리
3. 검색 가중치(alpha) 운영값: 고정 vs 카테고리별 동적 조정
4. SLA 기준: 우선순위(P1/P2/P3)별 목표 응답시간 차등 적용 여부

---

## 부록 A) 현재 구현과 PRD 매핑 (요약)

- 프론트엔드는 대시보드/VOC/가이드/승인 화면 구조를 이미 보유
- Prisma 스키마에 User/VOC/Guide/Approval/Document 모델 정의 완료
- 백엔드는 하이브리드 검색 + 생성 API를 제공
- 단, 운영 관점에서 인증/보안/계약 테스트/게시 자동화는 추가 구현 필요

